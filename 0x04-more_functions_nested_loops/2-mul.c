#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: an input integer.
 * @b: an input integer.
 * Return: product of @a and @c.
 */
int mul(int a, int b)
{
	return (a * b);
}
